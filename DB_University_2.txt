QUERY 1 

SELECT COUNT(*) AS `n_students`, YEAR(`enrolment_date`) 
FROM `students`
group by YEAR(`enrolment_date`)
order by `n_students` DESC;
----------------------------------------------------------------------------------------------------------------------------------------------------

QUERY 2

SELECT count(*) as `n_teachers`, `office_address` 
FROM `teachers` 
group by `office_address` 
order by `n_teachers` DESC;
----------------------------------------------------------------------------------------------------------------------------------------------------

QUERY 3

SELECT count(*) as `n_exams`, `exams`.`id`, AVG(`exam_student`.`vote`) as `avg_vote` 
FROM `exams` join `exam_student` on `exams`.`id` = `exam_student`.`exam_id` 
group by `exams`.`id` order by `avg_vote` DESC;
----------------------------------------------------------------------------------------------------------------------------------------------------

QUERY 4

SELECT `departments`.`name` as `nome_dipartimento`, `degrees`.`id` as `numero_esami` 
FROM `departments` 
join `degrees` on `departments`.`id` = `degrees`.`department_id` 
GROUP by `departments`.`id`;

